import { DataCell, Datum } from './types';
export interface TooltipProps<RawDatum extends Datum> {
    cell: DataCell<RawDatum>;
}
export declare const CellTooltip: <RawDatum extends Datum>({ cell }: TooltipProps<RawDatum>) => JSX.Element;
//# sourceMappingURL=CellTooltip.d.ts.map