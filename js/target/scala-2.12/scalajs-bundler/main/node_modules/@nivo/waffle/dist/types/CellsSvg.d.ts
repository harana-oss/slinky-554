import { Cell, ComputedDatum, Datum, EmptyCell } from './types';
interface CellsSvgProps<RawDatum extends Datum> {
    cells: EmptyCell[];
    computedData: ComputedDatum<RawDatum>[];
    cellSize: number;
    origin: {
        x: number;
        y: number;
    };
    borderWidth: number;
    getBorderColor: (cell: Cell<RawDatum>) => string;
}
export declare const CellsSvg: <RawDatum extends Datum>({ cells, computedData, cellSize, origin, borderWidth, getBorderColor, }: CellsSvgProps<RawDatum>) => JSX.Element;
export {};
//# sourceMappingURL=CellsSvg.d.ts.map